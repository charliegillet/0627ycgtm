```json
{
  "id": "C12",
  "source_idea": "R2-8 TRIBUNAL",
  "working_title": "Multi-agent fault attribution ('which agent broke the deal?')",
  "user": "Head of RevOps running multiple autonomous AI agents (SDR/pricing/onboarding) in the revenue pipeline",
  "pain": "When a deal stalls across 3 AI agents, no one can determine which agent made the wrong call, when, or why — human accountability structures don't exist for multi-agent pipelines (P10 ROI/accountability).",
  "trigger": "Voice query: 'Which agent broke the [X] deal?'",
  "core_loop": ["Trigger: spoken fault query", "Observe: TrueFoundry Gateway logs every agent action/cost/decision; Nova async pulls all three timelines", "Decide: Moss cross-indexes the 3 agent timelines, finds the error-propagation fork", "Act: render a live fault tree (failing branch lights up) + counterfactual ('if pricing agent used $85k at step 7...')", "Measure: mean-time-to-fault-attribution", "Learn: fault pattern library -> preemptive 'this pipeline matches a known failure topology'"],
  "tracks": ["Revenue Autopilot(the failing pipeline IS the automation)", "Reading Minds(what agents 'thought')", "Zero to One(multi-agent accountability tooling)", "Sales Cyborgs(humans regain oversight)"],
  "primary_metric": "Mean time to fault attribution in multi-agent revenue pipelines",
  "closest_products": ["LLM observability (Langfuse/Arize — traces, not revenue-fault attribution)", "no multi-agent revenue accountability tool exists"],
  "wedge": "MOAT: TrueFoundry as the single chokepoint all agent actions flow through + Moss cross-agent fault tree at sub-10ms + a compounding fault-pattern library. New category (multi-agent accountability) emerging exactly as agent pipelines proliferate.",
  "demo_moment": "JAW-DROP: 'The SDR agent broke it at 2:14pm Mar 3 — it used the pricing agent's $92k output which had confidence 0.41, with no fallback. Here's the token.' A red node lights up. An AI blames another AI, precisely.",
  "critical_dependencies": ["TrueFoundry structured decision logs Moss can ingest", "fault-tree rendering", "counterfactual generation"],
  "fatal_flags": ["HIGH build risk / SCOPE: needs 3 agent pipelines + gateway logs + tree before value — de-risk with seeded synthetic logs", "must still clear feasibility gate"],
  "score_history": [],
  "status": "R2_GATED_TOP6"
}
```
