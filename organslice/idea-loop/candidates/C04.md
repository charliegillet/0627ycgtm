```json
{
  "id": "C04",
  "source_idea": "P1",
  "working_title": "Evidence-backed post-call CRM write-back",
  "user": "Account Executive + RevOps manager",
  "pain": "Tools record calls but DON'T write back to CRM; 45 min/call of manual MEDDIC entry; 76% CRM data inaccurate; AI amplifies dirty data (P2, P3).",
  "trigger": "Sales call ends (LiveKit session terminates)",
  "core_loop": ["Trigger: call ends", "Observe: Moss ingests transcript, semantic-matches to approved MEDDIC field defs + past deal notes", "Decide: which fields to set, with confidence", "Act: Nova async writes CRM fields w/ confidence + diff view; AE 1-click confirm; manager Slack ping (TrueFoundry MCP)", "Measure: manual CRM minutes eliminated, field accuracy", "Learn: improve extraction from corrections"],
  "tracks": ["Sales Cyborgs", "Revenue Autopilot(write-back triggers downstream)", "Reading Minds(MEDDIC extraction = intent classification)"],
  "primary_metric": "Manual CRM minutes eliminated per rep per week (secondary: field accuracy)",
  "closest_products": ["Gong (records, no auto write-back)", "Salesforce Einstein (no grounded diff/confidence)", "Momentum/Scratchpad (templated, no confidence+source)"],
  "wedge": "Auto write-back with per-field confidence + a source-grounded diff the rep approves — closes the exact loop Gong leaves open.",
  "demo_moment": "Call ends; CRM fields auto-populate field-by-field with confidence; AE clicks Confirm once; manager Slack ping lands. Zero typing.",
  "critical_dependencies": ["transcript quality", "CRM write API (or mock CRM)", "MEDDIC field-definition seed", "Moss matching"],
  "fatal_flags": ["confirm gate before any write that leaves the tool", "diff must show source quote per field (no black-box writes)"],
  "score_history": [],
  "status": "GATED_TOP6"
}
```
